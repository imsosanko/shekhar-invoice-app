# Copy this file to .env to customize (all optional — sensible defaults are built in)
PORT=3000
JWT_SECRET=change-this-to-a-long-random-string
ADMIN_DEFAULT_PASSWORD=admin123
# PYTHON_BIN=python3   # uncomment and adjust if your system uses e.g. "python" instead
